import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'IBM Plex Mono', sans-serif`,
    body: `'IBM Plex Mono', 'Raleway', sans-serif`,
  },
});

export default theme;
